# PlainFacts Mobile Starter

Set your API base before running:

```bash
EXPO_PUBLIC_PLAINFOFACTS_API=http://YOUR_SERVER:8000 npm install
npm run start
```

This is a starter mobile shell that consumes the same `/briefs` API as the web app.
